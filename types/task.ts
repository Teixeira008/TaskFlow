export type Task = {
    id:string
    title:string
    completed:boolean 
    created_at:string    
}

/*Isso significa:

* `id` → sempre string
* `title` → sempre texto
* `completed` → sempre boolean
* `createdAt` → sempre número (timestamp)*/